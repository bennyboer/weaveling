[package]
name = "weaveling-test-harness"
version.workspace = true
edition.workspace = true

[dependencies]
sqlx = { workspace = true }
time = { workspace = true }
tokio = { workspace = true, features = ["rt"] }
uuid = { workspace = true, features = ["v4"] }

[features]
postgres = []

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt", "rt-multi-thread"] }
